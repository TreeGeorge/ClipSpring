<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="ActorMapper">

    <!-- Beans 클래스의 객체이름(id)과 클래스이름(type)을 명시한다. -->
    
        <resultMap id="actorMap" type="study.spring.clip.model.Actor">

        <!-- Beans의 멤버변수(property)이름과 대상 테이블의 컬럼(column)을 연결한다. -->
        <result property="movie_no" column="movie_no" />
        <result property="name" column="name" />
		<result property="actor_no" column="actor_no" />
    </resultMap>
	
	   <!-- 배우정보 -->
    <select id="actorMovie" parameterType="study.spring.clip.model.Actor"
        resultMap="actorMap">
        select name from actor
		where actor_no IN(select actor_no from movie_actor where movie_no=#{movie_no}); 
    </select>



</mapper>