<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MovieCateogoryJoinMapper">

	<!-- Beans 클래스의 객체이름(id)과 클래스이름(type)을 명시한다. -->
	<resultMap id="MovieCategoryJoinMap"
		type="study.spring.clip.model.MovieCategoryJoin">
		<!-- Beans의 멤버변수(property)이름과 대상 테이블의 컬럼(column)을 연결한다. -->
		<result property="movie_no" column="movie_no" />
		<result property="name" column="name" />
		<result property="thumbnail" column="thumbnail" />
		<result property="price" column="price" />
		<result property="sale" column="sale" />
		<result property="category_type_no" column="category_type_no" />
	</resultMap>

	<select id="oneMovieCategoryJoin"
		parameterType="study.spring.clip.model.MovieCategoryJoin"
		resultMap="MovieCategoryJoinMap">
		SELECT movie_no, name, thumbnail, price, sale FROM movie
		where movie_no IN(select movie_no from movie_category
		where category_type_no = #{category_type_no})
		LIMIT 0,9;
	</select>

</mapper>
	